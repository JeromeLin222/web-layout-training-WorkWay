<%
  // 透過頁面變數決定顯示哪個版本
  const isLoggedIn = locals.userState === 'logged-in';
%>

<% if (isLoggedIn) { %>
  <%- include('header-user.ejs') %>
<% } else { %>
  <%- include('header-guest.ejs') %>
<% } %>